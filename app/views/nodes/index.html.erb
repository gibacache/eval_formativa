<%= index_title %>

<table class="table table-striped">
  <thead>
    <tr>
      <th>id</th>
      <th>Etiqueta</th>
      <th>Pregunta</th>
      <th>Árbol</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nodes.each do |node| %>
      <tr>
        <td><%= node.id %></td>
        <td><%= node.label %></td>
        <td><%= node.questionable.question %></td>
        <td><%= node.tree.label %></td>
        <%= render 'shared/index_actions', object: node %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to_new_path %>
